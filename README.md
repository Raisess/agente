# Agente

To use this project, follow these steps:

1. **Setup Environment**:
   - Ensure Rust and Cargo are installed on your system.
   - Clone the project from `https://github.com/Raisess/agente` and then `cd ./agente`.

2. **Installing to your system**:
   - Execute the `install.sh` script and it will be installed.
