[
  {
    "type": "function",
    "name": "explore",
    "description": "Explore the project contents, folders and files.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "The path to explore"}
      },
      "required": ["path"]
    }
  },
  {
    "type": "function",
    "name": "read",
    "description": "Read a file content.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "The path of the file to read"}
      },
      "required": ["path"]
    }
  },
  {
    "type": "function",
    "name": "write",
    "description": "Write content to a file.",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "The path of the file to read"},
        "content": {"type": "string", "description": "The content to write to the file"}
      },
      "required": ["path", "content"]
    }
  },
  {
    "type": "function",
    "name": "search",
    "description": "Search for info on the web.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "The search query text"}
      },
      "required": ["query"]
    }
  },
  {
    "type": "function",
    "name": "fetch",
    "description": "Fetch a web page from a url, normally used after the search tool.",
    "parameters": {
      "type": "object",
      "properties": {
        "url": {"type": "string", "description": "The URL to be fetched"}
      },
      "required": ["url"]
    }
  },
  {
    "type": "function",
    "name": "bash",
    "description": "Execute bash commands in the host machine.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {"type": "string", "description": "The command to be executed in plain bash"}
      },
      "required": ["command"]
    }
  }
]
